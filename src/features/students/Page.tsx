import { ModulePage } from '../../components/common/ModulePage';

export function StudentsPage() {
  return <ModulePage title="Students Module" bullets={["MVP workflow", "Role-based access", "Firestore-backed operations", "Reporting and analytics"]} />;
}
