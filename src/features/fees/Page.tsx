import { ModulePage } from '../../components/common/ModulePage';

export function FeesPage() {
  return <ModulePage title="Fees Module" bullets={["MVP workflow", "Role-based access", "Firestore-backed operations", "Reporting and analytics"]} />;
}
