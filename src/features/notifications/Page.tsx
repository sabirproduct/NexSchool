import { ModulePage } from '../../components/common/ModulePage';

export function NotificationsPage() {
  return <ModulePage title="Notifications Module" bullets={["MVP workflow", "Role-based access", "Firestore-backed operations", "Reporting and analytics"]} />;
}
