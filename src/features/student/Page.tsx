import { ModulePage } from '../../components/common/ModulePage';

export function StudentPage() {
  return <ModulePage title="Student Module" bullets={["MVP workflow", "Role-based access", "Firestore-backed operations", "Reporting and analytics"]} />;
}
