import { ModulePage } from '../../components/common/ModulePage';

export function ExamsPage() {
  return <ModulePage title="Exams Module" bullets={["MVP workflow", "Role-based access", "Firestore-backed operations", "Reporting and analytics"]} />;
}
