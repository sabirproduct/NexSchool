import { ModulePage } from '../../components/common/ModulePage';

export function DashboardPage() {
  return <ModulePage title="Dashboard Module" bullets={["MVP workflow", "Role-based access", "Firestore-backed operations", "Reporting and analytics"]} />;
}
