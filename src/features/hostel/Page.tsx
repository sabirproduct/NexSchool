import { ModulePage } from '../../components/common/ModulePage';

export function HostelPage() {
  return <ModulePage title="Hostel Module" bullets={["MVP workflow", "Role-based access", "Firestore-backed operations", "Reporting and analytics"]} />;
}
